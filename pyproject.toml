[project]
name = "evidence-rag-agent"
version = "0.1.0"
description = "Evidence-aware RAG and SQL tool-use agent for business risk analysis"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
evidence-agent = "evidence_agent.agent:main"
evidence-server = "evidence_agent.server:main"
evidence-eval = "evidence_agent.eval:main"

[tool.pytest.ini_options]
pythonpath = ["."]
